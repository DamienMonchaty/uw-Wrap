# Database Configuration
# Set USE_MYSQL=true to use MySQL, false or undefined to use SQLite
USE_MYSQL=false

# SQLite Configuration (used when USE_MYSQL=false)
DB_FILE=./database.sqlite

# MySQL Configuration (used when USE_MYSQL=true)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=uwrap_db

# JWT Configuration
JWT_SECRET=your-very-secure-secret-key-change-this-in-production

# Server Configuration
PORT=3000
